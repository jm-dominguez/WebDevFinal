import { Meteor } from 'meteor/meteor';
import "../imports/api/routes.js";
import "../imports/api/searchs.js";
import "../imports/api/comments.js";
Meteor.startup(() => {
  // code to run on server at startup
});
